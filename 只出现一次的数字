public class Test {
    public static void main(String[] args) { 
        int[] a={1,2,2,3,4,4,5,3,5,8,8};

        int flag;
        for (int i = 0; i <= a.length-2; i++) {
            flag=1;
            for (int j = 0;j <=a.length-1 ; j++) {
                if(a[i]==a[j]&&i!=j){
                    flag=0;
                }
            }
            if(flag==1){
                System.out.println(a[i]);
            }
        }
    }
}

//给定一个非空整数数组，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。
